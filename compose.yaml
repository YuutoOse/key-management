services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: key_mgmt
      POSTGRES_PASSWORD: key_mgmt_pass
      POSTGRES_DB: key_management
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
